homeassistant:
  # Name of the location where Home Assistant is running 2
  name: Home
  # Location required to calculate the time the sun rises and sets
  latitude: !secret latitude
  longitude: !secret longitude
  # Impacts weather/sunrise data (altitude above sea level in meters)
  elevation: 40
  # metric for Metric, imperial for Imperial
  unit_system: metric
  # Pick yours from here: http://en.wikipedia.org/wiki/List_of_tz_database_time_zones
  time_zone: Europe/Stockholm
  #Customize

  customize: !include customize.yaml
  
  auth_providers:
    - type: homeassistant

my:

energy:

zone: !include zones.yaml

# Show links to resources in log and frontend
# introduction:

# Enables the frontend
frontend:

#Enable config menu
config:

#Enable map
map:

# zwave:
#   usb_path: /dev/serial/by-id/usb-0658_0200-if00
# #  usb_path: /dev/ttyACM0
#   network_key: !secret zw_network_key

group: !include groups.yaml

http:
  use_x_forwarded_for: true
  trusted_proxies:
    - !secret http_trusted_proxy

# Checks for available updates
updater:

# Discover some devices automatically
discovery:

# Allows you to issue voice commands from the frontend in enabled browsers
#conversation:
#dialogflow:

recorder: !include recorder.yaml

# Enables support for tracking state changes over time.
history:

logger:
  default: warning
#  logs:
#    RFXtrx: debug
#  logs:
#    homeassistant.components.zwave: debug

# View all events in a logbook
logbook:

# Track the sun
sun:

rfxtrx: !include rfxtrx.yaml

sensor: !include sensors.yaml

binary_sensor: !include binary_sensors.yaml

media_player: !include media_players.yaml

device_tracker: !include devices.yaml

#mqtt: !include mqtt.yaml
#mqtt:

camera: !include cameras.yaml

switch: !include switches.yaml

#light: !include lights.yaml

automation: !include automations.yaml

scene: !include scenes.yaml

notify: !include notifiers.yaml

alert: !include alerts.yaml

rest_command: !include rest_commands.yaml

shell_command: !include shell_commands.yaml

tradfri:
  allow_tradfri_groups: true

influxdb: !include influxdb.yaml

script: !include scripts.yaml

python_script:

cloud:

system_log:

netatmo: !include netatmo.yaml

timer: !include timers.yaml

aarlo:
  username: !secret ar_username
  password: !secret ar_password
  refresh_devices_every: 2
  stream_timeout: 120

alarm_control_panel:
  - platform: aarlo
    away_mode_name: armed
    home_mode_name: Home
    night_mode_name: night
    trigger_time: 30
    alarm_volume: 8

system_health:

life360: !include life360.yaml

#tplink: !include tplink.yaml

input_boolean: !include input_booleans.yaml
input_text:
input_select: !include input_selects.yaml
input_number: !include input_numbers.yaml
input_datetime:
counter:
  
ffmpeg:

stream:

svt_play:

mobile_app:

twilio:
  account_sid: !secret tw_account_sid_from_twilio
  auth_token: !secret tw_auth_token_from_twilio

nordpool: